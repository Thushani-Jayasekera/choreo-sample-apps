// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url = "mysql://avnadmin:AVNS_K_L-iI6blqfKKfs9Vga@mysql-readinglist-readinglist.aivencloud.com:14887/defaultdb?ssl-mode=REQUIRED" // uses connection pooling
  // directUrl = env("POSTGRES_URL_NON_POOLING") // uses a direct connection
}

model Book {
  id        String     @default(cuid()) @id
  title     String
  author    String
  status    String
  user_writer_id String
}
